"""Application version and operator-facing notes (keep in sync with frontend/package.json when bumping)."""

__version__ = "3.0.0"

HOSTING_WARNING = (
    "Before you start: Render free services can sleep, so the first request "
    "after inactivity can be slow. That is normal on free hosting."
)

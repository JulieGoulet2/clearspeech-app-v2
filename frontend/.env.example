# Backend API base URL (no trailing slash). Copied into the client bundle at build time.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Admin token for bypassing rate limits during local development only.
# WARNING: never set this in production (Vercel etc.) — NEXT_PUBLIC_ values are visible in the JS bundle.
# NEXT_PUBLIC_ADMIN_TOKEN=your-secret-token

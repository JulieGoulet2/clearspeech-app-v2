/**
 * next.config.ts — Next.js configuration for the ClearSpeech frontend.
 *
 * Currently uses default settings. Add options here if you need to configure
 * redirects, image domains, environment variables, or build behaviour.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
